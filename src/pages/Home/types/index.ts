export type TMarkerData = {
  CENTER: [number, number];
  ENGTYPE: string;
  NAME: string;
  TYPE: string;
  VARNAME: string;
};

export type TGeoKey = "" | "province" | "district";
export type TMarkerKey = TGeoKey;
